# openharmony多智能体项目

## 1. 概括

由总控编排器驱动的、受约束的多角色智能体安全分析系统。用户只需要提出服务、Socket 或源码仓库目标；总控编排器把任务交给源码定位、暴露面识别、可达性复核、调用图审查、漏洞分析、攻击复核和动态验证等角色。每个角色只能使用被允许的工具，并必须以结构化证据把结果交给下一个角色。

~~~text
用户目标
   ↓
总控编排器：拆任务、控预算、控状态、控安全门
   ↓
专业智能体：在自己的工具范围内观察、判断、行动
   ↓
证据与结构化产物：作为共享“黑板”
   ↓
确定性校验：过滤无效结果，决定能否进入下一阶段
   ↓
下一位专业智能体或最终报告
~~~

## 2. 系统中的三类参与者

### 2.1 总控编排器

总控编排器不负责亲自判断某一行代码是否存在漏洞，它负责管理整个任务：

- 把用户目标转换为阶段任务；
- 按依赖关系启动或跳过阶段；
- 为每个模型阶段绑定模型、预算、并发数和重试策略；
- 保存 checkpoint、日志、费用和阶段状态；
- 检查输入输出是否符合 schema；
- 发现错误时降级、重试或暂停等待用户确认；
- 确保未经确认的 Git 拉取、设备写操作或后续分析不会发生。

因此，编排器更像“项目经理和安全门”，而不是漏洞分析员。

### 2.2 智能体角色分工协作

专业智能体只在自己的职责和工具边界内工作。例如源码定位智能体可以搜索和读取 OpenGrok 代码，但不能直接执行任意 shell；Stage 2 智能体可以查找函数定义和调用关系，但不能修改源码。

### 2.3 确定性工具和执行器

解析器、调用图构建器、BFS、证据校验、HDC 适配器、Git 执行器和 Web Session 管理器不是智能体。它们不做开放式推理，而是执行可复现、可审计的规则和操作。

## 3. 暴露面识别智能体

### 3.1 基于大模型的自主暴露面识别

它根据暴露面类型和具体的Socket服务名称，然后通过 HDC 命令来制定计划采用agentic loop与鸿蒙暴露面rag知识来获取以下字段：

```json
{
  "暴露面类型": "Unix Domain Socket (UDS)",
  "套接字路径": "/dev/unix/socket/paramservice",
  "套接字类型": "STREAM",
  "权限配置": {
    "DAC权限": "0666 (srw-rw-rw-)，属主:root，属组:root",
    "SELinux标签": "u:object_r:paramservice_socket:s0"
  },
  "通信协议": "AF_UNIX",
  "关联进程": "init (PID: 1)",
  "运行状态": "LISTENING",
  "关键风险点": [
    "DAC权限0666导致任意进程可读写该套接字，存在未授权访问风险",
    "关联进程为root权限的init进程，攻击者可通过套接字注入恶意数据，提升权限或控制系统",
    "全局可读写结合高权限进程，扩大了攻击面，可能被用于本地提权攻击"
  ],
  "风险等级": "高"
}
```

- Socket 是否存在、类型和运行状态；
- 关联进程、PID、UID；
- DAC 权限、属主和属组；
- SELinux 标签；
- init 服务配置和运行参数。

HDC 适配器只接受固定命令模板，记录退出码、标准输出、错误输出、耗时和截断标记。模型不能生成任意设备命令。

### 3.2 异常处理

**服务存在但未启动**：
     **/dev/unix/socket/hiprofiler_unix_socket** 对应的 hiprofilerd 被配置为停止状态，不是listening,最初容易被误判
     为“Socket 不存在”。后来增加了“已安装但未启动”的状态，并要求用户同意后才临时启动再侦查。以下是socket服务启动命令

```
 hdc shell param set hiviewdfx.hiprofiler.profilerd.start 1
```

  执行后应重新探测 /dev/unix/socket/hiprofiler_unix_socket，确认其是否进入 LISTENING 状态。

如果设备上找到了服务配置，但当前服务没有运行，模块不会自动启动：

~~~text
发现配置但未运行
   ↓
等待用户决定
   ├─ 拒绝：保留停止状态，输出已有事实
   └─ 同意：重新读取配置和参数
              ↓
          仍为 0 → 1？
              ├─ 是：执行固定启动参数，再次只读侦查
              └─ 否：不写设备，返回部分结果
~~~

### 3.3 LLM 语义提取

可选模型只在已经采集的事实中归纳“暴露面类型、状态、进程、协议、权限和风险”等字段。每个字段必须引用已存在的证据；模型返回格式错误、证据不存在或调用失败时，系统回退到确定性基线，并把无法确认的字段保留为未知。



## 4. 基于opengrok源码定位智能体

### 4.1 接受的输入信息

它可以看到：

- 用户的**系统服务**原始描述和目标；
- **OpenGrok 搜索结果**；
- 已读取的代码片段和**函数源码**；
- **证据** ID、文件路径、行号和已有候选；
- Manifest 或仓库元数据；
- 用户对上一次候选的接受、拒绝和补充约束。

### 4.2 工作流程

~~~text
标准化目标（如/dev/unix/socket/hiprofiler_unix_socket）
   ↓
选择下一步工具：搜索完整路径、搜索定义、搜索符号、列目录或读文件
   ↓
读取工具结果
   ↓
判断证据缺口：宏展开、服务端初始化、客户端连接、仓库映射等
   ↓
继续搜索，或形成候选并请求用户确认
~~~

   工具                 用途
  ━━━━━━━━━━━━━━━━━━━
   **search_full**          全文检索字符串或表达式，例如 Socket 路径、函数调用、日志内容

   **search_definition**    查找宏、常量、函数或类型定义，例如 SOCKET_NAME 的定义

   **search_symbol**        按符号查找声明、定义和引用，例如 CreateSocket、InitService

   **search_path**          按目录或路径查找相关源码文件

   **read_file**            读取 OpenGrok 中指定源码文件，获取更完整的上下文

 ━━━━━━━━━━━━━━━━━━━

### 4.3 输出

定位智能体最终形成：

- 目标规格；
- 服务端和客户端源码证据；
- 证据关系图；
- OpenHarmony 仓库候选、URL 和 revision；
- 用户确认记录；
- 拉取后的路径、版本和符号校验；
- 交给普通扫描器的源码交接信息。

即使“绑定或获取 Socket”等谓词没有找到，也不会由硬编码规则直接终止；缺失项会展示给用户，由用户决定接受、拒绝或带反馈继续检索。





## 5. 源码漏洞扫描

用户确认源码仓库，或直接提供本地仓库后，总控编排器创建扫描任务。静态扫描不是一个单一智能体，而是一组前后衔接的角色。

### 5.1 源码静态解析与调用图构建

使用静态解析器（Tree-sitter或joern）和语言解析器提取**函数单元、函数索引**、**入口提示和源码范围**；**调用图构建器**根据 AST 和符号信息生成原生调用边，并把函数指针、宏、表驱动和 IPC/IDL 间接关系记录为 residual。

这一层不依赖模型，是后续智能体共同使用的事实基线。

### 5.2 可达性复核智能体

在 processing_level=reachable 时，可达性角色检查结构化入口是否完整。开启 LLM 复核后，模型分批查看函数单元，补充：

- **entry_point：可能的外部入口；**
- **external_input：接收外部输入的函数；**
- **cross_process：参与跨进程、异步或消息边界的函数。**

确定性准入逻辑随后处理模型信号：

- high 信号可以作为额外 BFS 种子；
- medium 信号保留对应单元，但不扩散 BFS；
- low 信号不改变可达范围，但可保留审计信息。

模型负责提出语义信号，BFS 和准入门禁仍由程序执行。

### 5.3 调用图语义智能体

OpenHarmony 调用图语义阶段由多个相互独立的审查角色组成：

1. **调用图恢复角色**：审查解析器标记的残余间接调用点；
2. **候选边复核角色**：对解析器已有候选边进行第二次语义核对；
3. **调用边投影角色**：只把满足证据和置信度门槛的边写入独立 semantic overlay；
4. **分派码证据角色**：从源码和头文件提取 IPC/SA 分派选择值。

前两个角色可以调用模型，投影和结果合并由确定性代码完成。原生 call_graph.json 不会被模型静默覆盖。

### 5.4 上下文增强智能体

上下文增强角色以某个函数单元为中心，按需读取：

- 上游调用者和下游被调用者；
- 函数所在文件和相邻源码；
- 输入如何经过参数、容器、回调和成员函数传播；
- 权限检查、边界检查、生命周期和并发保护；
- Socket、Binder、NAPI、HDF/HDI 等平台边界；
- 调用图中已经确认的语义边和未知关系。

agentic 模式允许它**多轮调用受限读文件和索引工具**；single-shot 模式只做一次批量上下文生成。**它负责丰富 dataset_enhanced**，不负责直接下漏洞结论。

  1. **get_static_dependencies**
     **获取当前分析单元的静态被调用函数和调用者，返回原始名称及已解析的函数 ID。**

  2. **search_usages**
     **搜索指定函数在仓库中的所有调用位置，主要用于向上追踪调用者。**

  3. **search_definitions**
     **搜索指定函数的定义；如果没有精确匹配，则返回相似函数。**

  4. **read_function**
     **根据函数 ID 读取完整函数源码、起止行号、函数类型和所属类名。**

  5. **list_functions**
     **列出指定源码文件中定义的所有函数，帮助理解文件结构。**

  6. **read_file_section**
     **按起始行号和结束行号读取文件中的指定源码片段，用于查看函数前后文。**

  7. **finish**
     **提交当前单元的最终增强结果，包括纳入上下文的函数、使用场景、安全分类、分类理由和置信度。**

## 7. 漏洞分析智能体

 通常由多个并行的 worker 组成，每个 worker 处理一个分析单元。它们共享同一份应用上下文、安全基线和单元上下文，但彼此不进行自由对话。

每个 worker 需要分别回答：

1. **目标函数**做了什么；
2. **输入**来自哪个外部或半可信边界；
3. **输入**经过了哪些转换、容器、调用和错误路径；
4. **是否存在缺陷**；
5. **是否可达**；
6. 影响是崩溃、资源耗尽、信息泄露、越权、状态破坏还是其他类型；
7. 现有 guard 是否真正覆盖危险路径；
8. **证据是否足够**，哪些内容仍然未知。

结果只能是 safe、protected、vulnerable 或 inconclusive，并且必须符合严格的结构化字段约束。提示词要求模型覆盖 IPC 输入校验、权限、内存、整数、解析器、并发、生命周期、Socket/NAPI/HDF/HDI、注入和业务逻辑等类别，不能只寻找一种攻击载荷。

## 8.攻击者视角的证据验证智能体

接收漏洞分析智能体产出的漏洞候选，从攻击者角度重新验证：

  - **外部输入能否真正到达目标函数；**
  - **是否存在可控输入到危险操作（sink）的完整路径；**
  - **权限、参数校验、生命周期和并发保护是否有效；**
  - **攻击前置条件、漏洞影响和利用路径是否成立；**

该智能体使用有限工具：

- **search_usages：查真实调用点和数据传播；**
- **search_definitions：查下游函数、宏和类型定义；**
- **read_function：读取完整函数上下文；**
- **list_functions：确认文件中可用的函数范围；**
- **finish：提交复核结论。**

它重点检查“模型是否漏看了调用边、sink、权限 guard、错误路径或下游处理”。如果证据不足，应该保持 inconclusive，而不是为了完成任务强行改成 safe。

## 9. 动态验证和报告（可选）

### 9.1 动态验证

目前的 Claude 动态验证模式，是一个“静态分析结果驱动的交互式验证流程”。

  它首先读取 之前智能体 筛选出的可验证漏洞候选，生成一个独立任务工作区，其中包含**源码、调用上下文、静态分析产物、候选清单和 OpenHarmony 动态验证 Skill。**随后由 Claude Code 在 Web 终端中执行：

  1. **检查源码和漏洞前置条件**；
  2. **检查 HDC、工具链和开发板连接**；
  3. **按任务要求进行构建、签名、安装和有限触发**；
  4. 收集日志、进程状态、崩溃信息和其他运行时证据；
  5. **为每个候选写入 verdict.json、notes.md、commands.jsonl 等结果文件。**

  Web 页面通过 PTY/SSE 实时显示 Claude 的对话、命令和验证过程。该模式不会自动对整个仓库进行无限制测试，也不会默认修改源码；需要用户提供已授权的源码、工具链和开发板环境。最终结果通常用于判断漏洞是否确认、未复现、环境阻断或证据不足。



动态结果使用 CONFIRMED、NOT_REPRODUCED、BLOCKED 和 INCONCLUSIVE 等状态，不能把“没有复现”直接解释为“安全”。

### 9.2 报告生成

报告角色读取统一结果，生成：

- 漏洞摘要；
- 文件、函数、行号和调用链；
- source 到 sink 的数据流；
- 影响、前置条件和证据；
- 修复建议或可直接应用的修复代码；
- 中文和英文披露文档。

报告角色不能补造缺失证据。无法生成安全修复代码时，应明确标记需要维护者确认的接口、权限或下游实现。
