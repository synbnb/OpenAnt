# VulnFounder 项目流程处理逻辑说明

本文是项目全流程的简化说明，重点解释各阶段如何接收输入、形成判断、交接证据以及处理不确定性。它不展开具体源码文件、内部路径或实现代码，适合项目汇报、方案介绍和快速了解系统运行方式。

## 一、总体处理思路

VulnFounder 不是一次性把整个仓库交给大模型，而是把安全分析拆成一条可追踪的证据链：

1. 先确认设备或用户指定的暴露面是什么；
2. 再确认暴露面对应的源码服务和扫描范围；
3. 然后建立函数、调用关系和入口之间的结构联系；
4. 只把有可解释来源的函数送入后续分析；
5. 由 Stage 1 判断潜在安全问题，由 Stage 2（如果启用）进一步复核；
6. 最后把结论、源码、调用路径、证据和不确定性统一整理成报告。

整个流程把“设备事实”“源码归属”“静态可达”“输入是否能影响危险操作”和“安全结论”分开记录。前一层的结果可以作为后一层的依据，但不能直接替代后一层。例如，设备上存在一个监听端点，不代表已经找到了正确的源码实现；函数在调用图中可达，也不代表外部数据一定能传到危险参数。

系统的基本原则是：确定性工作由程序完成，跨文件和跨机制的语义判断由大模型辅助完成，关键外部操作由用户确认，证据不足时保留未知而不是强行给出安全结论。

## 二、两种启动方式

系统可以从设备暴露面开始，也可以从本地源码仓库开始。

从设备开始时，流程先发现端点，再识别端点信息，随后定位源码并进入仓库扫描。从仓库开始时，可以跳过设备发现和源码定位，直接进入范围确认、解析、可达性和漏洞分析。

无论从哪种方式开始，后续扫描都需要记录源码版本、扫描范围、分析选项、调用图版本和模型配置。这样才能知道某个结论是在哪一份源码和哪一套规则下得到的。

## 三、开发板暴露面发现

这是可选的前置阶段，目的是建立某一块开发板自己的暴露面资产清单。

系统首先确认设备是否在线，然后由设备探测智能体围绕“找出所有处于监听、绑定或可识别状态的 Socket 暴露面”建立任务。任务不是只执行一组固定命令，而是根据当前结果动态增加查询，例如：

- 发现一个端点后，继续查询它对应的协议、状态、进程、用户和权限；
- 发现进程后，继续查询进程的文件描述符、状态和安全域；
- 发现命名 Socket 后，补充路径属性和服务标识；
- 发现 TCP/UDP 端口后，补充地址族、传输协议、监听状态和进程归属；
- 某条命令没有提供足够证据时，任务树会增加针对缺失字段的补查任务。

大模型可以根据当前任务树、已执行命令和知识指南选择下一条设备命令。命令执行结果先保存为原始事实，再由解析器和模型共同提取结构化字段。模型可以识别新的输出格式，但不能把没有出现在设备结果中的字段当成事实。

本阶段的输出是设备资产记录，包括端点名称或地址、类型、状态、协议、关联进程、进程身份、DAC 权限、SELinux 信息以及证据来源。每台设备独立保存历史记录，后续可以从资产记录中选择一个目标进入源码定位。

如果服务已经安装但没有运行，系统只提示可能的启动动作，并等待用户确认。未经确认不会改变设备状态；用户拒绝或无法启动时，仍保存静态探测结果，并把“未运行”作为前提条件。

## 四、单目标暴露面识别

用户可以输入命名 Unix Socket，也可以输入 TCP/UDP 的进程、地址和端口。系统先把输入标准化，区分路径型端点和网络型端点，补齐可推断的协议、地址族和查询目标。

随后，设备智能体围绕这个目标收集事实，而不是把所有设备信息混在一起。它会检查目标是否存在、当前状态是什么、由哪个进程持有、进程以什么身份运行、端点具有什么权限，以及是否能关联到安全策略信息。

结果中要区分三类内容：

1. 设备直接返回的事实，例如监听状态、端口、权限和进程号；
2. 根据多个事实归并出的字段，例如同一进程对应多个文件描述符；
3. 风险解释，例如高权限进程配合宽松权限可能扩大本地攻击面。

风险解释不能反过来修改事实。如果某字段没有查到，应显示未知或未确认，而不是用常见值填充。

## 五、源码定位与服务归因

源码定位的输入是标准化后的端点及设备侧证据。系统先建立检索身份，再按由具体到一般的顺序检索：端点名称、配置或服务标识、进程名称、注册宏、创建或获取逻辑、协议处理符号、入口读取函数和构建映射。

检索过程由大模型进行有界循环：模型查看当前证据，选择搜索定义、搜索引用、读取源码或查找调用关系等动作；程序校验动作格式、查询长度、路径范围、证据引用和预算；工具返回结果后进入下一轮。重复动作、无效证据引用、超出范围的查询和无法解释的结论会被拒绝或要求修正。

证据归因至少分为以下几类：

- 端点身份：源码中的名称、地址、配置项或服务标识是否与目标一致；
- 服务端实现：哪里创建、绑定、获取或接收该端点；
- 消费者处理：哪个函数读取消息、解析请求并进入业务逻辑；
- 客户端或控制端：哪些代码连接或发送请求，用于区分客户端仓库和服务端仓库；
- 构建与仓库映射：当前实现属于哪个仓库、版本和服务目录。

如果多个仓库都出现相关名称，系统不会只选择第一个搜索命中，而是比较进程构建归属、服务端证据、客户端证据以及重复或拆分源码关系。大模型负责解释候选之间的差异，程序负责保存每个候选的证据和评分依据。

当只找到配置名称而没有找到业务接收链时，结果可以是“仓库归属较可信、入口证据不完整”。这比伪造一个入口函数更可靠。仓库拉取、版本选择和扫描范围扩大等会改变外部状态的操作，需要用户确认。

## 六、扫描范围确认

拿到仓库后，系统不会默认把所有目录都当成同等重要。它先根据端点、服务注册关系、进程归属和入口函数调查完整服务目录，再给出建议扫描范围。

用户可以选择整个仓库，也可以选择包含完整服务调用链的子目录。若选择子目录，系统仍会根据符号引用、注册关系、工厂返回类型、构建依赖和必要的头文件关系进行受控扩展。范围过窄会造成上游或下游缺失，范围过宽会增加解析和模型成本，这两种风险都会在扫描记录中说明。

## 七、源码解析与调用事实建立

解析阶段先建立函数、类型、调用表达式、注册关系、线程入口、IPC 分派和命令映射等基础索引。快速语法解析负责尽可能覆盖全量源码；具备编译上下文时，Clang 等语义前端用于补充类型、重载、声明定义关系和直接成员调用。缺少真实构建上下文时，系统可以使用受限重建配置，但必须标记其来源和适用范围，不能把手工配置当成产品构建事实。

每个调用站点都进入统一台账，并分别记录：静态绑定是否成功、动态目标是否完整、目标是否在扫描范围内、构建条件是否明确、证据来自哪种解析器以及当前处理状态。这样可以区分“没有发现调用”“发现调用但无法绑定”“已绑定但定义在范围外”和“已绑定但尚未写入图”等不同问题。

原始解析结果保留用于审计，有效调用图则由经过验证的事实生成。语义恢复、Clang 补充和后续复核可以增加有效边，但不能静默覆盖原始证据。回调、工厂、虚函数、线程、异步任务、IPC 和命令表分别记录关系类型，不把所有关系都当作普通同步函数调用。

当一个关系已经具备充分的类型、签名、源码位置和构建条件证据时，程序可以自动生成有效边；只有“当前暂时发现了一个候选目标”但无法证明目标集合完整时，仍保留为候选关系。未决关系会进入补证任务，原因和预算耗尽状态不会被改写成不可达。

## 八、可达性筛选

可达性阶段回答的是：“在当前源码范围、调用图版本和入口策略下，目标函数是否存在一条从入口到它的静态路径？”它不是漏洞判断，也不是数据流或可利用性判断。

系统先识别结构入口和语义入口，例如主程序入口、线程入口、IPC 接收、Socket 接收、事件回调、命令分派和其他明确的外部边界。大模型返回的 `high` 或 `medium` 只是入口证据的一部分，只有信号格式、入口身份和来源证据通过准入后，才可参与传播。

可达结果分层保存：

- `strict`：由已验证入口和已验证调用边组成的路径；
- `candidate`：由较弱但有依据的入口或候选边形成的待复核路径；
- `fallback`：由于图或入口证据不足而暂时保留，不能解释为已证实可达。

高置信入口可以作为严格路径种子；中置信入口可以沿已验证边扩展到候选集合，但不自动升级为严格入口。下游函数不要求每个函数都单独得到大模型入口信号，只要存在合格的上游路径，就应继承对应的可达状态。

调用图恢复应在最终筛选前完成，并在恢复或投影后重新计算可达性。没有严格种子时可以启用明确标记的保留策略，防止静默漏分析，但评测和报告必须单独统计这些保留对象。

## 九、上下文构造与增强

对于每个进入后续分析的目标，系统构造以目标函数为中心的安全上下文，同时保留入口到目标的有序源码路径。路径通常按照“外部入口或调度入口 → 消息处理或业务分派 → 中间封装 → 目标函数”的顺序排列，并在函数之间使用清晰边界。

上下文至少包含目标源码、调用路径中的关键函数、入口来源、调用边证据、权限和输入校验、候选分支以及尚未解决的断点。调用链完整性关注执行关系；参数如何从输入传播到危险操作属于更细的数据流问题，除非已有可靠证据，不应在 Stage 1 中伪装成已经证明。

上下文增强阶段由模型按需读取函数索引和源码，补充注册关系、调用者、调用目标、状态变化、异步关系、依赖缺口和风险相关摘要。增强可以发现新的事实，但新事实需要经过统一证据校验后才能影响严格调用图；不能因为增强阶段提到了某个函数，就直接把它当作确定调用边。

## 十、Stage 1 初步安全分析

Stage 1 接收目标函数、入口路径源码、调用关系证据、威胁模型和上下文增强结果。它只对指定目标函数进行安全判断，但必须利用上游路径判断外部边界是否能到达该函数。

模型分别评估四个问题：

1. 目标函数中是否存在缺陷，例如未校验输入、危险调用、越界、空指针、权限缺失、竞态或资源失控；
2. 哪些外部或半可信输入能到达相关参数、对象或状态；
3. 缺陷可能造成什么影响，例如崩溃、资源耗尽、信息泄露、状态破坏、权限绕过或命令执行；
4. 当前证据是否足以作出安全、受保护、存在问题或未决判断。

权限检查不能代替输入校验，客户端限制不能代替服务端限制，缺少完整攻击载荷也不能自动得出安全结论。若关键下游实现、入口或影响证据缺失，应保留 `inconclusive` 或明确的候选风险。

Stage 1 的修复建议是分析参考，不以“证据不足”为理由完全空缺；但建议中的接口、权限名和代码细节必须标明假设，不能把推测写成已经验证的补丁。

## 十一、Stage 2 证据复核

Stage 2 是可选的攻击者视角复核阶段。它针对 Stage 1 的 finding 使用受限源码工具继续查找调用者、定义、使用点、入口、校验、类型和状态条件，并检查 Stage 1 是否把客户端路径、无关路径或错误的动态目标当成了攻击链。

它可以把初步风险升级、降级或保持未决，也可以补充缺少的源码证据。Stage 2 不会把没有证据的结论强行变成确定漏洞；同样，Stage 2 未完成也不等于 Stage 1 的风险不存在。两阶段结果和依据分别保存，便于比较模型判断与复核结果。

## 十二、动态验证

在设备、构建和权限条件具备时，系统可以执行动态验证，检查服务是否运行、端点是否可连接、分派代码是否匹配、请求是否能进入处理逻辑以及日志或返回值是否符合预期。

动态验证通常由隔离的执行代理完成。代理先核对设备和源码版本，再按照证据允许的范围执行命令或测试。动态失败可能来自服务未启动、设备状态、权限、网络或构建差异，不能直接推翻静态证据；动态成功也只能证明该环境下的行为，不能自动推广到所有配置。

## 十三、报告生成

最终报告首先展示用户最关心的内容：漏洞或风险说明、具体触发行、入口到触发点的调用链、影响、根因和修复要点，以及按调用顺序排列的源码片段。

之后再展示原始元数据、仓库和版本、证据引用、模型判断、Stage 2 状态、动态验证状态、缺失证据、置信度和复现信息。报告必须区分：

- 已由源码直接支持的事实；
- 模型基于证据作出的语义判断；
- 仍未验证的候选关系或假设；
- 仅用于后续调查的补证任务。

如果一个上下文包含多个危险点，报告生成不能只保留其中一个而丢弃其他线索；应分别列出目标调用点或明确说明尚未逐点验证。修复代码属于建议性内容，生成时要保留适用条件和需要维护者确认的部分。

## 十四、Web 会话与产物交接

每次扫描都有独立会话，按阶段记录运行状态、参数、模型、耗时、成本、日志、证据、检查点和失败原因。阶段之间通过版本化产物交接，而不是依赖页面上的临时状态。

Web 页面展示的是这些阶段产物的可视化视图：设备资产、源码定位证据、初始检索、调用图、可达性、上下文、Stage 1、Stage 2、动态验证和报告。用户可以查看中间结果、暂停或取消任务、复用有效检查点、删除会话或重新运行失败阶段。

某个阶段失败时，系统应保留已经成功的阶段和失败原因，并在能力允许时回退到较弱模式。例如模型暂时不可用时，可以保存设备原始结果；OpenGrok 暂时不可用时，可以保留标准化目标和待重试任务。回退不能伪装成完整成功，页面需要明确显示哪些阶段完成、哪些阶段未执行。

## 十五、评测与反馈闭环

评测数据与运行时证据分开保存。目标样本只用于事后核对，不能在生产扫描中直接告诉模型目标函数、标准攻击链或正确答案，否则会产生评测泄漏。

评测至少分开统计：暴露面字段识别正确率、源码仓库定位正确率、入口函数召回率、严格可达率、候选可达率、Stage 1 风险对应率、Stage 2 复核变化以及报告证据完整性。`inconclusive` 但风险方向与样本一致时，可以作为“风险线索命中”单独统计，不能与已确认结论混为一谈。

Stage 2、上下文增强和动态验证发现的新关系，应转化为可复查的事实、待办任务或回归测试。只有经过核验的新事实，才可进入统一调用图和后续可达性计算。这样系统会形成“扫描发现缺口 → 补充证据 → 重新计算 → 复核结果 → 固化测试”的闭环，而不是依赖人工为某个样本编写特殊路径。

## 十六、关键边界总结

为了避免误解，整个流程可以用下面几句话概括：

1. 设备探测回答“现在有什么暴露面”；源码定位回答“它属于哪里”；静态扫描回答“代码中有哪些可能路径”；安全分析回答“目标函数是否存在有证据支持的风险”。
2. 证据图、调用图、参数数据流和动态行为是不同层次，不能因为其中一层成立就替代其他层。
3. 大模型可以提出检索动作、语义关系和风险解释，但所有关键关系都需要保留来源、适用条件和不确定性。
4. 严格路径、候选路径和兜底保留必须分开统计；未解析、未分析或预算耗尽不能写成不可达或安全。
5. 每个阶段都应能回答“输入是什么、做了什么、依据是什么、输出交给谁、失败后怎么办”。这也是报告可复现和专家审查的基础。
