# WEB-05A 报告与摘要中英文输出测试记录

日期：2026-08-23  
阶段：Web/报告阶段 A  － 本地化报告、移除品牌文案、增加中文入口

## 本阶段修改

- 保留英文 `report.html`、`report-reskin.html` 和 `SUMMARY_REPORT.md` 路径，兼容已有链接。
- 新增 `report.zh-CN.html`、`report-reskin.zh-CN.html` 和 `SUMMARY_REPORT.zh-CN.md`。
- 摘要增加中文专用提示词 `report/prompts/summary.zh-CN.txt`，并支持 `--language zh-CN`。
- HTML 两套 Go 模板均支持中文标签、图表标签、阶段/状态/判定翻译。
- 删除 HTML 中 `Powered by Knostic` 和 `Generated by OpenAnt` 文案；Python 备用 HTML 生成器的旧 footer 也已移除。
- Web 首页和扫描详情页在中文文件实际生成后提供中文摘要/报告链接。
- 旧扫描若没有中文文件不会伪造链接；新扫描完成时会尝试生成中文文件，失败不影响英文报告完成。

## 自动化测试

### Go

命令：

```text
GOCACHE=... GOPATH=... go test ./...
```

结果：通过。`cmd`、`internal/report`、`internal/server` 等全部包通过；共覆盖报告模板解析、中文渲染、品牌文案删除、中文路径恢复与 HTTP 服务、Web 链接存在性等检查。

### Python

命令：

```text
python -m pytest -q \
  tests/report/test_localized_summary.py \
  tests/openharmony/test_report_platform_context.py \
  tests/test_pr69_round3.py \
  tests/test_report_verdict_findingless.py \
  tests/report/test_sibling_result_iteration_guard_fa16.py \
  tests/report/test_poisoned_results_substrate.py
```

结果：`24 passed`。

覆盖内容包括：

- 中文摘要确实加载中文提示词，不会误用英文模板；
- 中文提示词包含安全分析表格和 OpenHarmony 上下文要求；
- 原有摘要、报告数据、结果归一化和 OpenHarmony 报告上下文测试未回归；
- Python 备用 HTML 输出不再包含旧 OpenAnt footer。

## 构建

已重新构建：

```text
apps/openant-cli/bin/openant
```

## 说明

本记录的测试使用离线适配器验证中文提示词和模板，不会产生真实模型费用。完整扫描时，中文摘要会额外调用一次报告阶段模型；存在需要修复的问题时，中文 HTML 修复建议会额外调用一次 `report-data`。静态 HTML 标签和图表本身不增加模型调用。
