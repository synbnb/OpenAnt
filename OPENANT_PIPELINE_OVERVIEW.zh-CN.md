#  流程简介

##  Joern 不能保证 OpenHarmony 调用图完整，原因主要有：

  1. 解析语法不等于解析全部调用关系
     Tree-sitter 和 Joern 都可以识别函数、变量和显式调用，例如：

     return HandleRequest(data);

     但下面这些关系需要额外语义推断：
      - 函数指针；
      - 虚函数和继承分派；
      - 宏展开后的调用；
      - 模板实例化；
      - 注册表或数组保存的回调；
      - 字符串映射到函数；
      - 运行时插件加载。
     
  2. C/C++ 预处理和编译配置很复杂
     OpenHarmony 大量使用宏、条件编译、Build GN 配置、平台变体和生成文件。Joern 需
     要正确的编译参数、头文件路径、宏定义和源码版本。如果这些信息不完整，它同样可
     能：
     
      - 看不到某些条件编译分支；
      - 无法解析头文件中的类型；
      - 无法判断宏展开结果；
      - 无法连接跨文件或跨仓库的符号。
     
  3. OpenHarmony 有大量“注册驱动”的关系
     很多服务不是直接调用处理函数，而是通过：

     transaction code
         → Stub::OnRemoteRequest
         → handler table / switch / baseFuncs_
         → 具体处理函数

     baseFuncs_[enable_sensor]

     或者：
     
     服务初始化
         → 注册回调
         → 事件循环
         → 运行时触发回调
     
     这种关系不一定表现为普通的 C/C++ 函数调用。Joern 能识别其中一部分静态关系，但
     不能天然理解 OpenHarmony 的 Binder、SA、IDL、HDF/HDI 或特定注册框架语义。
     
     
     
     Joern 仍然是静态分析工具，无法单独证明这些运行时条件。

## 1. 项目做什么

1. 找到需要分析的 OpenHarmony 源码；
2. 识别外部输入能够到达的服务入口和函数；
3. 为函数补充调用上下文和安全边界；
4. 判断是否存在漏洞；
5. 在条件允许时进行运行时验证；
6. 生成用户可以阅读和复核的报告。

## 2. 总体流程

~~~text
用户输入源码路径或服务描述
        │
        ├─ 已有源码路径 ─────────────────────┐
        │                                    │
        └─ 不知道仓库 ─→ OpenGrok 源码定位 ─→ 用户确认并拉取源码
                                             │
                                             ▼
                                    统一进入扫描流程
                                             │
        参数与环境检查
              ↓
        源码解析、函数索引和调用图
              ↓
        入口识别与可达性筛选
              ↓
        应用上下文和 OpenHarmony 安全基线
              ↓
        可选的 LLM 入口/调用图语义补充
              ↓
        Agentic 上下文增强
              ↓
        Stage 1 漏洞分析
              ↓
        可选 Stage 2 结果验证
              ↓
        统一结果 pipeline_output.json
              ↓
        可选动态测试
              ↓
        中英文摘要、HTML 和漏洞披露文档
~~~

其中，源码定位是可选的前置流程；动态测试也是可选的后置流程。没有候选漏洞
时，动态测试会正常跳过，而不是把扫描视为失败。

## 3. 用户可以怎样运行

### Web

启动本地 Web：

~~~text
./apps/openant-cli/bin/openant serve --addr 127.0.0.1:8080
~~~

Web 适合查看实时日志、阶段状态、历史扫描、阶段产物和报告，也可以在
Claude Code 动态测试模式下直接进行交互。

### Go CLI

Go CLI 负责项目、配置、任务和 Python 扫描器的编排。常用命令是：

~~~text
./apps/openant-cli/bin/openant scan <源码目录>
~~~

### Python 分阶段命令

Python 核心支持把解析、上下文、增强、分析、验证、动态测试和报告拆开运行。
分阶段运行适合调试某个阶段，完整扫描适合日常使用。

## 4. 可选前置阶段：源码定位

当用户只知道“要分析哪个服务”或“哪个 socket”，但不知道对应的源码仓库时，
可以先使用源码定位器。

### 4.1 定位过程

源码定位器会依次完成：

1. 从自然语言中提取服务名、socket 路径或其他目标关键词；
2. 检查 OpenGrok 是否可用；
3. 搜索路径、宏、函数定义和调用位置；
4. 读取关键文件，形成带文件和行号的证据；
5. 区分服务端、客户端和测试代码；
6. 根据 OpenHarmony Manifest 将源码路径映射到 GitCode 仓库；
7. 把候选仓库、目标目录、证据和警告展示给用户；
8. 等待用户确认；
9. 用户确认后才拉取仓库；
10. 拉取后检查仓库来源、版本、源码路径和目标符号。

模型可以帮助决定下一步检索什么，但不能自行执行 Git 拉取，也不能凭空生成
仓库地址。最终使用的仓库映射必须有 Manifest 或其他可验证证据。

### 4.2 用户确认

确认页面应重点查看：

- 找到的服务端源码文件；
- 客户端或调用方文件；
- 关键证据的文件路径和行号；
- GitCode 项目、版本和本地落盘目录；
- 尚未确认的证据和版本风险。

拒绝时可以填写原因，定位器会保留已有证据并带着反馈继续检索。删除定位会话
只删除会话产物，不删除已经拉取到 OpenAnt/source_code_base 的源码。

## 5. 扫描阶段

### 阶段 1：初始化

扫描器首先检查源码路径、输出目录、平台、语言、模型配置和可选开关，然后
创建本次任务的阶段记录。Web 任务使用独立的扫描 ID，因此重复扫描不会覆盖
历史结果。

### 阶段 2：源码解析

解析器遍历受支持的源码文件，使用 joern(Tree-sitter) 或对应语言的 AST 逻辑提取：

- 函数和方法；
- 参数、返回值和源码位置；
- 函数之间的调用关系；
- 结构化入口标志；
- 文件和平台元数据。

主要产物是 dataset.json、analyzer_output.json 和 call_graph.json。默认的
reachable 模式会从识别到的入口执行 BFS，只把入口能够到达的函数交给后续
分析；all 模式则保留全部解析单元。

OpenHarmony 解析还可能生成 semantic_graph.json、
call_graph_residuals.json 等平台诊断产物。原生调用图由解析器建立，是确定性
事实来源。

### 阶段 3：应用上下文

扫描器优先读取仓库提供的安全模型；没有安全模型时，再由模型根据仓库内容
生成上下文。随后合并 OpenHarmony 平台最低安全基线。

上下文主要说明：

- 系统组件的用途；
- 哪些边界接收外部输入；
- 攻击者可以控制什么；
- 哪些身份、权限和隔离规则需要检查；
- 哪些行为是正常业务功能。

结果写入 application_context.json，供漏洞分析和结果验证使用。

### 阶段 4：语义补充（可选）

为了弥补纯结构解析可能遗漏的入口或间接调用，用户可以开启：

- LLM 可达性复核：复核全部函数，补充可能的入口信号，再重新做可达性筛选；
- OpenHarmony 调用图恢复：根据残余诊断、函数索引和源码证据寻找候选调用边；
- 候选边复核：检查解析器已经找到的候选关系；
- 调用边投影：把高置信、带证据的新增关系用于增量扩大可达性范围；
- 分派码证据：提取 Binder/服务分派或动态测试需要的常量证据。

这些阶段默认只产生附加证据和 overlay，不直接覆盖原生 call_graph.json。
投影的目标是增加召回范围，而不是裁掉原有函数。

### 阶段 5：Agentic 上下文增强

增强器以当前 dataset、函数索引、调用关系、源码和安全上下文为输入，为每个
分析单元补充：

- 上游调用者和下游被调用者；
- 入口到目标的传播关系；
- 外部输入和信任边界；
- 权限检查、参数校验和危险操作；
- OpenHarmony IPC、SA、IDL、NAPI、HDF/HDI 等线索。

增强结果写入 dataset_enhanced.json。它丰富漏洞分析使用的上下文，但不会把
模型推断直接伪装成原生调用图事实。

### 阶段 6：Stage 1 漏洞分析

模型逐个分析函数单元，重点回答四个问题：

1. 是否存在真实缺陷；
2. 外部输入是否能够到达；
3. 可能造成什么安全影响；
4. 当前证据是否足够完整。

审查范围不局限于越权或代码执行，也包括：

- IPC 输入校验；
- 空指针、越界、UAF 和其他内存安全问题；
- 整数和尺寸错误；
- 资源耗尽、服务崩溃和拒绝服务；
- 并发、生命周期和回调问题；
- 文件、socket、NAPI、HDF/HDI 和 ioctl 边界；
- 信息泄露、状态破坏、类型和 ABI 错误。

主要产物是 results.json。每个结果都应包含判断理由、数据流、证据、反证、
缺失证据、攻击场景和置信度。

结果一般分为：

- safe：没有证据支持相关缺陷；
- protected：存在风险点，但已有具体防护覆盖危险路径；
- vulnerable：缺陷、可达性和安全影响都有证据支持；
- inconclusive：关键证据不足，暂时不能可靠定性。

### 阶段 7：Stage 2 结果验证（可选）

开启验证后，系统重点复核 vulnerable、bypassable 和 inconclusive 单元。
验证器可以搜索函数引用、定义、完整函数和函数列表，再从攻击者视角检查：

- Stage 1 的调用关系是否正确；
- 权限检查是否真的覆盖危险操作；
- 输入是否能继续传播到 sink；
- 缺失的下游实现是否改变结论；
- 是否存在可重现的攻击路径。

结果写入 results_verified.json。Stage 2 可以把待定结果变成安全、受保护或
漏洞，也可以保留为未决；它不会在缺少证据时伪造确定结论。

### 阶段 8：统一输出

系统将当前有效的 Stage 1 或 Stage 2 结果、源码片段、调用关系、阶段报告、
平台画像和跳过原因合并为 pipeline_output.json。

这是动态测试、报告生成和 Web 友好视图共同使用的统一输入。

### 阶段 9：动态测试（可选）

动态测试只对经过前置阶段筛选的候选执行，不会因为没有候选而制造测试载荷。
当前有两种模式：

- Docker 模式：在隔离环境中生成并执行测试，返回确认、未复现、阻塞或待定；
- Claude Code 模式：准备任务工作目录，在 Web 对话窗口中由 Claude Code
  使用源码、工具和候选清单进行验证。

Claude Code 任务会看到：

- 源码路径；
- 前置扫描产物；
- 候选问题；
- hdc、HAP 构建/签名等工具说明；
- 动态测试技能文档。

运行结果应写入任务目录的 results/，之后由 Web 服务回填统一输出并重新生成报告。

### 阶段 10：报告

报告器读取 pipeline_output.json 和动态测试结果，生成：

- 漏洞摘要；
- 中英文 Markdown 报告；
- HTML 报告；
- 每个问题的独立披露文档。

披露条目会展示文件路径、函数、漏洞类型和简要影响；用户仍可以进入详情
查看源码、数据流和证据。

## 6. Web 中如何查看结果

Web 页面将扫描拆成阶段菜单。每个阶段显示：

- 当前状态；
- 阶段目的；
- 关键统计；
- 主要产物；
- 原始 JSON；
- 更适合阅读的中文友好视图。

大文件采用分页、搜索、折叠和按需加载。dataset 可以按函数单元搜索，调用图
可以按入口、层级和节点展开，避免把整个大型 JSON 一次性塞进页面。

顶部日志显示所有阶段的实时决策和错误；历史扫描从每个独立的扫描目录恢复，
重启 Web 不会清除历史结果。

## 7. 主要产物在哪里

Web 扫描通常保存在：

~~~text
~/.openant/webui/<scan-id>/
~~~

最需要关注的文件是：

| 文件 | 重点 |
| --- | --- |
| platform_profile.json | 平台和源码范围 |
| dataset.json | 解析后的函数单元 |
| call_graph.json | 原生函数调用关系 |
| application_context.json | 攻击者、输入源和安全边界 |
| dataset_enhanced.json | 增强后的函数上下文 |
| results.json | Stage 1 漏洞分析 |
| results_verified.json | Stage 2 验证结果 |
| pipeline_output.json | 跨阶段统一结果 |
| dynamic_test_results.json | 动态验证结果 |
| scan.report.json | 全流程状态、耗时和成本 |

不是每次都会有全部文件。关闭某个可选阶段、没有候选漏洞或阶段失败时，系统
会在阶段报告和 scan.report.json 中写明原因。

## 8. 一个简单例子

用户想分析一个 OpenHarmony socket，但不知道仓库：

~~~text
我想分析 /dev/unix/socket/paramservice。
~~~

系统先在 OpenGrok 中搜索路径和宏，读取参数服务的初始化和客户端/服务端代码，
再从 Manifest 映射到 startup_init 仓库。用户确认后，源码被拉取到
OpenAnt/source_code_base/startup_init，随后进入普通扫描。

普通扫描会：

1. 解析参数服务函数和调用图；
2. 识别 socket 初始化、消息处理和外部输入边界；
3. 生成 OpenHarmony 安全上下文；
4. 可选地用模型补充遗漏的入口或调用关系；
5. 对可达函数进行 Stage 1 分析；
6. 对高风险或待定结果进行 Stage 2 验证；
7. 有条件时执行 Docker 或 Claude Code 动态测试；
8. 输出统一 JSON、Web 视图和报告。

## 9. 阅读结果时的三个原则

1. 先看 pipeline_output.json 和 scan.report.json，确认最终结果和阶段是否完整；
2. 对具体漏洞回到 results 或 results_verified，查看函数、证据、数据流和缺失证据；
3. 对调用关系回到 call_graph.json、semantic graph 和增强上下文，区分解析器事实、
   语义候选边和模型解释。

OpenAnt 的结果是自动化安全分析和证据整理，不替代人工复核。尤其是
inconclusive、动态测试阻塞、调用图残余和版本不一致的结果，应保留为需要
进一步确认的事项。
