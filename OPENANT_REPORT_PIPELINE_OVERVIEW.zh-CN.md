# 漏洞挖掘智能体全流程介绍

## 一、项目目标

面向 OpenHarmony 及其他大型 C/C++ 软件仓库，自动完成从源码准备、
入口识别、调用关系分析，到漏洞发现、复核和报告生成的全过程。

它的核心思路不是只搜索危险函数，而是回答一条完整的问题：

> 外部输入能否进入某个功能，经过哪些函数和安全检查，最终是否会造成安全影响？

系统将确定性代码分析和大模型语义分析结合起来。确定性程序负责提取事实，
大模型负责理解复杂语义、补充遗漏线索和解释安全影响，二者相互约束，避免
完全依赖规则或完全依赖模型猜测。

## 二、总体流程

```text
用户提出分析目标
      ↓
准备并确认源码仓库
      ↓
源码解析与函数索引
      ↓
调用关系和入口识别
      ↓
可达性筛选与语义补充
      ↓
建立应用安全上下文
      ↓
补充分析单元上下文
      ↓
第一阶段漏洞分析
      ↓
第二阶段证据复核
      ↓
可选动态验证
      ↓
汇总结果并生成报告
```

其中，源码定位是可选的前置流程：用户已经有本地仓库时可以直接进入扫描；
只知道系统服务名称或 Socket 名称时，可以先让定位器寻找并拉取对应仓库。

## 三、源码仓库定位与准备（可选）

当用户不知道目标服务属于哪个代码仓库时，定位器支持用自然语言描述目标。
系统先提取服务名、Socket 路径、接口名称等关键词，再访问 OpenGrok 检索源码。

定位过程不是一次搜索就结束，而是一个有界的对话式循环：

1. 将用户描述标准化为明确的检索目标；
2. 搜索路径、字符串、宏、符号和目录；
3. 读取候选源码，确认服务初始化、监听、连接和消息处理关系；
4. 区分生产服务端、客户端以及测试或模糊测试代码；
5. 根据 OpenHarmony 工程元数据把源码目录映射到代码仓库；
6. 将关键证据、仓库版本和不确定性展示给用户；
7. 用户确认后才执行拉取；
8. 拉取完成后检查版本、目录和关键源码是否匹配，再交给扫描流程。

如果搜索证据不足、仓库地址失效或版本不匹配，系统会保留已有证据并给出候选
版本或替代仓库，不会把一次定位失败直接当作整个任务失败。用户拒绝候选结果时，
可以附带理由，系统会把反馈用于下一轮检索。

## 四、源码解析与结构化建模

扫描开始后，系统首先检查源码目录、语言、平台和运行配置，然后枚举需要分析的
源文件。生产代码与测试、模糊测试代码分开处理，避免测试样例被误认为真实服务
入口；但不会仅因为目录名称包含第三方、内核或生成代码就一概删除相关证据。

解析器使用 Tree-sitter 等语法分析技术提取函数和方法，形成统一的分析单元。
每个单元包含函数身份、源码范围、参数、返回值、所属类型以及入口提示等信息。
这一阶段主要由程序完成，不依赖大模型，因此结果稳定、可重复。

## 五、调用图与入口识别

系统根据语法树、符号声明和函数索引建立调用图，记录“谁调用谁”，同时建立
反向关系以便追踪调用者。调用图优先保存确定性解析结果，无法确认的间接调用
会被标记为残余关系，而不是强行生成错误边。

OpenHarmony 中常见的 IPC、System Ability、IDL、NAPI、Socket、服务注册和回调
关系可能无法完全由语法直接恢复。开启语义阶段后，大模型可以读取相关源码，
对残余调用点和候选边进行复核。模型只能提交带源码依据的补充关系，不能静默
覆盖原始调用图；通过门槛的关系以独立语义层参与后续可达性计算。

入口识别关注的是“外部或跨边界如何进入程序”，而不是简单寻找没有调用者的函数。
入口可能来自进程启动、IPC 分派、服务注册、Socket 接收、文件回调或导出接口。

## 六、可达性筛选

可达性阶段决定后续需要分析哪些函数：

- 选择“全部”时，保留所有解析单元，适合完整审计，但成本和耗时较高；
- 选择“可达”时，从识别出的外部入口沿调用图向下搜索，只保留可能被实际执行的单元；
- 如果没有可靠入口，系统采用保召回策略，宁可保留更多单元，也不把结果错误地裁成空集。

为减少规则遗漏，系统可以让大模型对函数集合补充语义入口信号。高置信度的
外部输入或跨进程信号可以帮助扩展入口范围；置信度较低的信号会保留为分析提示，
不会直接改变确定性调用图。所有信号都需要能够回指源码或结构化事实。

## 七、应用上下文与安全基线

漏洞判断不能脱离应用场景。系统会建立应用安全上下文，说明组件用途、攻击者
能力、外部输入来源、信任边界、权限要求和正常业务行为。

对于 OpenHarmony，系统额外采用平台最低安全基线，重点审查：

- IPC、MessageParcel、IDL 字段和接口令牌；
- 调用者身份、权限与隔离边界；
- 长度、数量、索引、空容器和空指针元素；
- 内存安全、整数计算、资源消耗和错误路径；
- Socket、文件、NAPI、HDF/HDI、ioctl 和异步回调；
- 并发、生命周期、状态机、信息泄露和数据完整性。

权限检查只能说明“谁可以调用”，不能代替输入校验、边界检查或生命周期保护。
即使是已授权的系统应用，也可能提交空、超大、畸形或状态不一致的参数。

## 八、分析上下文增强

对每个待分析单元，系统会补充它的上游调用者、下游被调用者、入口到目标的路径、
输入传播、权限检查、危险操作和不确定性。

增强模式下，大模型可以按需读取函数索引和相关源码，继续追踪解析器没有完整呈现
的调用关系。增强结果不会替换原始解析事实，而是作为更丰富的解释上下文提供给
漏洞分析阶段。这样，模型看到的不再只是一个孤立函数，而是一段有来源、有边界的
数据流和调用链。

## 九、第一阶段：漏洞发现

第一阶段对每个分析单元独立判断四件事：

1. 代码中是否存在缺陷；
2. 外部或跨边界是否能够到达缺陷；
3. 缺陷可能造成什么安全影响；
4. 当前证据是否足以得出结论。

分析范围覆盖命令或代码注入、权限绕过、输入校验缺失、空指针和越界、释放后
使用、整数溢出、资源耗尽、竞态和死锁、路径与文件问题、信息泄露、类型混淆、
状态机错误以及其他 OpenHarmony 常见安全问题。

模型不再要求必须构造完整的攻击载荷。空输入、空指针、边界值、重复请求、异常
状态和畸形结构同样可以构成有效攻击场景。权限检查也不会被误认为能够保护所有
下游输入。

每个单元最终得到四类结论之一：

- **安全**：没有足够证据支持相关缺陷；
- **已防护**：存在潜在风险，但具体防护覆盖了所有相关路径；
- **存在漏洞**：缺陷、可达性和安全影响均有证据支持；
- **待确认**：仍缺少关键调用边、下游实现、输入来源或影响证据。

## 十、第二阶段：证据复核

第二阶段主要处理第一阶段发现的高风险或待确认结果。验证器会围绕原有证据缺口
继续查找调用者、被调用者、定义、权限检查、输入传播和危险操作，并从攻击者视角
检查攻击条件是否成立。

这一阶段的作用不是简单重复第一阶段，而是把“可能存在”尽量转化为有证据的确定
结论，也允许模型推翻第一阶段的误报。无法补齐证据时会明确保留待确认状态，
不会为了得到确定数字而强行判定安全或漏洞。

## 十一、可选动态验证

如果用户开启动态验证，系统只把静态分析中仍具有验证价值的候选交给隔离环境。
动态验证可以使用容器或 Claude Code 工作区，由模型根据源码证据设计测试步骤，
执行后将结果分为已复现、未复现、环境阻塞、无法判断或执行错误。

动态验证不是静态分析的替代品：设备、权限、编译环境或输入条件不具备时，
“未复现”不能直接证明代码安全。所有动态操作限制在专用工作区，结果与源码和
静态产物分离保存，避免测试过程修改被分析仓库。

## 十二、结果汇总与报告

系统最后把解析、入口、调用关系、上下文、漏洞分析、复核和动态验证结果统一起来，
形成一份跨阶段结果。报告会展示：

- 总体扫描范围和完成状态；
- 各类漏洞数量、严重程度和置信度；
- 每个问题对应的源码位置、调用链、输入来源和危险操作；
- 已有防护、证据不足之处和复核结论；
- 动态验证状态、跳过阶段及失败原因；
- 模型调用量、耗时和成本等运行指标。

Web 页面以阶段卡片、实时日志、调用图、入口列表、关键结果和可折叠详情展示这些
内容。用户可以先看结论和风险摘要，再展开证据和源码，不必直接阅读大量原始数据。
同时保留原始阶段结果，保证每个结论都可以追溯到具体分析过程。

## 十三、可靠性与可复现性

整个流程采用阶段化设计。每个阶段都有独立状态、耗时、模型用量和错误记录；
阶段失败时会保留已经完成的结果，并明确说明后续哪些步骤被跳过或降级。大型仓库
支持检查点和断点续跑，网络波动或单批次模型调用失败不会自动抹掉已有结果。

确定性解析、调用图和结果汇总可以重复运行；大模型阶段通过固定的上下文、证据
引用、输出格式和预算约束提高稳定性。最终报告同时保留模型判断与程序事实，方便
人工复核、模型对比和后续验收。

## 十四、一句话总结

 将“源码定位、结构化解析、调用图恢复、可达性判断、上下文理解、漏洞分析、
证据复核和动态验证”串成一条可追踪流水线：程序负责可靠地整理事实，大模型负责
理解复杂关系，用户在关键仓库确认和高风险结论处保留最终控制权。
