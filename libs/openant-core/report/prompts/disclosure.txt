Generate a security disclosure document from the provided vulnerability data.

INPUT DATA:
{vulnerability_data}

The vulnerability data may contain an `report_context` object. Use it as
evidence when describing the exact file/line, source-to-sink flow, Stage-2
entry path, and native/semantic/recovered call-graph relationships. Do not
invent a function, edge, line number, or source statement that is not present
in that context. The final report layer appends the verbatim source and call
chain section deterministically, so you must not rewrite or reproduce source
code from memory.

OPENHARMONY PLATFORM CONTEXT (if present):
{platform_context}

Attacker model: {attacker_model}

OUTPUT FORMAT:

# Security Disclosure: {short_title}

**Product:** {product_name}
**Type:** CWE-{cwe_id} ({cwe_name})
**Affected:** {affected_versions}

## Summary

{2-3 sentence description of the vulnerability. What is the bug? What can an attacker do?}

{1 sentence explaining why the code is vulnerable — refer to the function name and the dangerous operation}

## Steps to Reproduce

**Prerequisites:**
{bullet list of what attacker needs}

**Step 1:** {action}

{code block if needed}

**Step 2:** {action}

{code block if needed}

[Continue for all steps]

**Tested:** {platform_version}, {analysis_date}.

## Impact

- {impact point 1}
- {impact point 2}
- {impact point 3}

## Suggested Fix

{1 sentence description}

```{language}
{fixed_code_snippet}
```

---

Verified via {verification_method}.

---

INSTRUCTIONS:
- Keep summary under 50 words
- Do NOT include a "## Vulnerable Code" section — it is added automatically after your output is generated. Do NOT emit code fences with the vulnerable source code. If you need to reference the vulnerable code, refer to the function name and describe the issue in prose.
- Other code snippets (Suggested Fix): show only the minimal change needed, not entire files
- Steps to Reproduce: if dynamic_testing data exists, use those steps; otherwise write "[REQUIRES DYNAMIC TESTING]" for each step
- When `report_context` is present, mention the target file and line range and
  summarize the ordered source-to-sink steps in the Summary or Steps section.
  Distinguish native local edges from semantic/recovered or cross-process
  evidence; absence of a native edge is not proof that a Binder/SA path is
  unreachable.
- Impact: 3-5 bullet points, no sub-bullets
- Suggested Fix: show minimal code change, use comments to indicate what was added
- Do not include CVSS scores or severity ratings
- Do not include disclosure timelines
- Do not include references section
