请根据提供的漏洞数据生成一份安全漏洞披露文档。除代码路径、函数名、CWE 编号、模型名称、状态枚举和其他必须保持原样的技术标识外，所有说明文字、字段标签、章节标题和结论都必须使用简体中文。

输入数据：
{vulnerability_data}

输入数据可能包含 `report_context` 对象。使用其中的证据描述目标文件、行号、源到汇数据流、第二阶段入口路径以及原生/语义/恢复调用图关系。不得编造上下文中不存在的函数、调用边、行号或源码语句。漏洞源码和调用链源码由报告层在模型生成后确定性追加，因此不要凭记忆重写或复制源码。

OpenHarmony 平台上下文（如果存在）：
{platform_context}

攻击者模型：{attacker_model}

输出格式：

# 安全漏洞披露：{short_title}

**产品：** {product_name}
**类型：** CWE-{cwe_id} ({cwe_name})
**影响版本：** {affected_versions}

## 摘要

用 2-3 句话说明漏洞是什么、攻击者可以造成什么影响。

再用 1 句话说明为什么代码存在漏洞，引用函数名和危险操作。

## 复现步骤

**前置条件：**
{preconditions}

**步骤 1：** 具体操作

（必要时提供代码块）

**步骤 2：** 具体操作

（必要时提供代码块）

[按需继续列出全部步骤]

**测试环境：** {platform_version}，{analysis_date}。

## 影响

- 影响 1
- 影响 2
- 影响 3

## 建议修复

用 1 句话说明修复方向。

```{language}
{fixed_code_snippet}
```

---

验证方式：{verification_method}。

---

要求：
- 摘要不超过 50 个字。
- 不要输出“## 漏洞代码”章节；该章节由报告层在生成后确定性加入。不要输出包含漏洞源码的代码块；如需提及源码，只引用函数名并用文字描述问题。
- “建议修复”中的代码块只能包含最小修复改动，不要复制整个文件。
- 如果没有动态验证数据，在复现步骤中明确写出“[需要动态验证]”。
- 存在 `report_context` 时，在摘要或复现步骤中提及目标文件和行号范围，并概括有序的源到汇步骤。区分原生本地调用边、语义/恢复调用边和跨进程证据；没有原生调用边不等于 Binder/System Ability 路径不可达。
- 如果 `review_status` 为 `needs_review`，或者第二阶段评估表明缺陷、可达性、影响或证据完整性未知/不完整，应将该条目描述为“尚未完成验证的问题”，并明确列出缺失证据。不要把它写成已确认漏洞、安全结论或误报。报告层会在无法证明仓库专用补丁时追加明确标注的修复参考模板；不得把该模板描述为已验证或可直接应用的修复。
- 影响部分列出 3-5 个条目，不要使用嵌套列表。
- 建议修复应展示最小代码改动，使用注释标明新增内容。
- 不要包含 CVSS 分数、严重性评级、披露时间线或参考文献章节。
- 不要在模板之外添加评论或解释。
